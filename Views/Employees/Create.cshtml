@model DB_s2_1_1.ViewModel.Employees.EmployeeModViewModel

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Employee</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Employee.FIO" class="control-label"></label>
                <input asp-for="Employee.FIO" class="form-control" />
                <span asp-validation-for="Employee.FIO" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Employee.StationId" class="control-label"></label>
                <select asp-for="Employee.StationId" class ="form-control" asp-items="@Model.Stations"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
