@model DB_s2_1_1.EntityModels.Train

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Train</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="RouteId" class="control-label">@Html.DisplayNameFor(model => model.RouteId)</label>
                <select asp-for="RouteId" class="form-control" asp-items="ViewBag.RouteId"></select>
            </div>
            <div class="form-group">
                <label asp-for="StationId" class="control-label">@Html.DisplayNameFor(model => model.Station)</label>
                <select asp-for="StationId" class ="form-control" asp-items="ViewBag.StationId"></select>
            </div>
            <div class="form-group">
                <label asp-for="CategoryId" class="control-label">@Html.DisplayNameFor(model => model.Category)</label>
                <select asp-for="CategoryId" class ="form-control" asp-items="ViewBag.CategoryId"></select>
            </div>
            <div class="form-group">
                <label asp-for="SeatsQty" class="control-label">@Html.DisplayNameFor(model => model.SeatsQty)</label>
                <input asp-for="SeatsQty" type="number" step="1" min="20" class="form-control" />
                <span asp-validation-for="SeatsQty" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
