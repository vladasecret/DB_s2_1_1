@model DB_s2_1_1.EntityModels.Timetable

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Timetable</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label name="TrainId" class="control-label">Train id</label>
                <select name="TrainId" class="form-control" asp-items="ViewBag.TrainId"></select>
            </div>
            <div class="form-group">
                <label name="ArrivalTime" class="control-label">Arrival time</label>
                <input name="ArrivalTime" required min="@DateTime.Now" type="datetime-local" class="form-control" />
            </div>
            <div class="form-group form-check">
                <label class="form-check-label">
                    <input class="form-check-input" type="checkbox" name="TrainDirection" value="@bool.TrueString" /> @Html.DisplayNameFor(model => model.TrainDirection)
                </label>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}